<?xml version="1.0" encoding="utf-8"?>
<!-- Выбранная карточка: та же плашка, но обведена акцентом. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/graphite_2" />
    <stroke
        android:width="2dp"
        android:color="@color/accent_solid" />
    <corners android:radius="10dp" />
</shape>
